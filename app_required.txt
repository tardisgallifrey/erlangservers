Required to correctly run application without rebar3:

Run erl inside person_system/apps/person_app:

code:add_path("ebin").

Compile to a path:

compile:file("src/person_server.erl", [{outdir, "ebin"}].

